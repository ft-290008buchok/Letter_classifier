#include "data_load.h"

using namespace sf;

int main()
{
    dataset DATA = load_dataset();
    //for (int i = 0; i < DATA.size_for_one_letter * 5 * 50 * 50; i++)
    //    std::cout << DATA.data[i];
    return 0;
}

